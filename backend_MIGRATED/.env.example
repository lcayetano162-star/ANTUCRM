# ============================================================
# AntuCRM Backend - Variables de Entorno
# ============================================================

# Puerto del servidor
PORT=3001

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=antucrm
DB_USER=postgres
DB_PASSWORD=password

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# JWT Secret (para autenticación)
JWT_SECRET=tu_jwt_secret_aqui

# ============================================================
# Configuración SMTP para envío de correos
# ============================================================

# Gmail SMTP (recomendado para desarrollo)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu_email@gmail.com
SMTP_PASS=tu_app_password_de_gmail

# Remitente por defecto
FROM_EMAIL=noreply@antucrm.com
FROM_NAME=AntuCRM

# ============================================================
# Otras configuraciones de SMTP (ejemplos)
# ============================================================

# SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=tu_sendgrid_api_key

# Outlook/Office365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu_email@outlook.com
# SMTP_PASS=tu_password

# AWS SES
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu_ses_username
# SMTP_PASS=tu_ses_password
